package com.mali.ds.dp.greedy;

/* https://leetcode.com/problems/walking-robot-simulation/
* medium */
public class WalkingRobotSimulation {

    /* main issue here is we need to check if any obstacle comes into our way
    * we know that in each step we can move upto 10 steps (from constraints in question)
    * so check if any obstacle comes in that way
    * stack or queue doesn't help here as we need to cross check every obstacle
    * hash map or set comes to our rescue with added extra space */
    public int robotSim(int[] commands, int[][] obstacles) {
        return 1;
    }
}
